# The Runway

Mobile-friendly debt payoff dashboard.

## GitHub Pages

Publish from the `main` branch and `/ (root)` folder under:

**Settings → Pages → Build and deployment → Deploy from a branch**

The website entry file is `index.html`.
